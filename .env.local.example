# Optional. Leave unset in prod — the Next.js app calls /api/query same-origin.
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Required at runtime (set these as Vercel environment variables too)
GROQ_KEY=your_groq_api_key_here
HF_TOKEN=your_huggingface_read_token_here
